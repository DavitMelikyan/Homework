#include <stdio.h>
int main() {
  int length = 0;
  int width = 0;
  printf("Please enter width and length of the rectangle:");
  scanf("%d" "%d", &length, &width);
  printf("The Area of your rectangle = %d", length * width);

  return 0;
}
